platform: linux

image_resource:
  type: docker-image
  source:
    repository: library/node
    tag: "latest"

inputs:
  - name: demo-react-repo

run:
  path: sh
  args:
    - -exc
    - |
      cd demo-react-repo
      npm test
